{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Supabase": {
    "// Url": "The self hosted GoTrue stack's base URL (supabase/docker-compose.yml, matching GOTRUE_PORT, e.g. http://localhost:9999 for a local docker compose up). Required at startup, see Program.cs.",
    "Url": "",
    "// AnonKey": "The ANON_KEY from supabase/.env (a real JWT signed with that file's JWT_SECRET, not the placeholder in .env.example).",
    "AnonKey": ""
  }
}
